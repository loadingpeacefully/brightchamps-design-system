# DC-005 Four-Way + DC-008 Brand Typo — Verification Screenshots

**Date:** 2026-04-26
**Trigger:** Source-code audit of `brightchamps-student-dashboard` (the real Next.js student app, 1,156 .tsx files) revealed `#722ED1` shipping in production (year-in-review, learn page header, FeedLayout) AND a typo `#4d3bc2` shipping in 46 files. DC-005 upgraded to four-way; DC-008 added as a new ticket.

| # | File | Shows |
|---|---|---|
| 01 | [01-dc005-four-way-conflict.png](2026-04-26-dc005-fourway-dc008/01-dc005-four-way-conflict.png) | Element shot of DC-005. CRITICAL pill, **4-WAY CONFLICT** badge (was 3-WAY). Four swatches in a row: Designer DS `#722ED1` / Production · Figma `#4e3bc2` / Production · Code (Feed) `#6651e4` / Production · Code (Dashboard) `#722ED1`. Red note links to both `docs/repo-analysis-student-dashboard.md` and `docs/component-spec-verification.md`. Updated full notes preserve the four-purple narrative + cross-reference DC-008 for the typo. |
| 02 | [02-dc008-brand-typo.png](2026-04-26-dc005-fourway-dc008/02-dc008-brand-typo.png) | Element shot of new DC-008. MEDIUM severity, ΔE 0.5 badge (sub-1 visually identical), ENGINEERING ACTION status pill. Title: "Brand primary typo — #4d3bc2 shipping in 46 files". Text-only Intended (#4e3bc2) / Shipping (#4d3bc2) comparison — no swatches because the values would be visually identical. Action + notes call out: invisible to the human eye, wrong in every computed style, fix before any CSS-variable codemod. |
| 03 | [03-dc-section-8-tickets.png](2026-04-26-dc005-fourway-dc008/03-dc-section-8-tickets.png) | Full Designer-conflicts section in context. Section header automatically reads "8 tickets" (up from 7). DC-005 stands out with thick red border (critical). DC-008 sits at the bottom as a medium-severity engineering-action ticket. |

## What changed

### `ledger/drift/designer-conflicts-2026-04-26.json` — DC-005
- `title`: "three-way conflict" → "four-way conflict"
- Added `productionCodeValue2: "#722ED1"` (the dashboard repo's shipping designer purple)
- Added `fourWayConflict: true` (preserves `threeWayConflict: true` for back-compat)
- `notes`: rewritten to describe four distinct purples shipping simultaneously, with file counts per repo

### `ledger/drift/designer-conflicts-2026-04-26.json` — DC-008 (new)
- `id`: DC-008, severity: medium, status: open
- `title`: "Brand primary typo — #4d3bc2 shipping in 46 files"
- `designerValue`: "#4e3bc2", `productionValue`: "#4d3bc2", `deltaE`: 0.5
- `action`: codemod #4d3bc2 → #4e3bc2 across the dashboard repo
- `notes`: invisible to the human eye but wrong in every computed style; must be fixed before CSS-variable codemod runs

### `src/publish/ds-site-v2/lib/designer-conflicts.ts`
- Added optional `productionCodeValue2?: string | null`
- Added optional `fourWayConflict?: boolean`

### `src/publish/ds-site-v2/app/(docs)/surfaces/page.tsx`
- `ConflictRow`: when `fourWayConflict === true && category === 'color'`, renders a 4-column grid of swatches (Designer DS / Production · Figma / Production · Code (Feed) / Production · Code (Dashboard))
- New "typo case" branch: when `category === 'color' && deltaE < 1`, renders a text-only Intended / Shipping comparison instead of swatches (because they'd be visually indistinguishable)
- Otherwise falls back to the existing 3-way and 2-way render paths
- Section header automatically reads `${conflicts.tickets.length} tickets` so it ticks up to 8

### Bonus: `src/publish/ds-site-v2/lib/componentSpecs.ts`
- All 5 components that match dashboard sources flipped from `inferred` to `conflict` with real `sourceFile` paths and detailed conflict lists
- Button: 9 conflicts (variant taxonomy / padding / radius / disabled bg / hover / hardcoded danger+info colors)
- ProgressLine: 8 conflicts (track + height + radius + in-progress fill + completed-green is a fourth value `#11ac69`)
- Accordion: 7 conflicts (border / title / status text use brand purple, not neutral)
- LessonList: 6 conflicts (radius / padding / divider / icon size all differ)
- Layout: 7 conflicts (sidebar is 104px chrome rail, NOT 240px; three-column not two-column; 8 layouts not 3 variants)
- GreenLine: stays `inferred` — no reusable component exists in production

### Documentation added
- [docs/component-spec-verification-dashboard.md](../component-spec-verification-dashboard.md) — full component-by-component verification against the dashboard repo (supersedes the older feed-repo audit at `docs/component-spec-verification.md`)

## Two new tickets recommended (not yet filed)

- **DC-009** — production progress green is `#11ac69`, not in ledger. Used in `src/components/ProgressBar/progressbar.module.scss` (default + completed fill, value text). Reused as `$completed-state` and `$success-green` in `_variables.scss`.
- **DC-010** — paused-state color `#3b9af5` not in ledger. Used in `ProgressBar.paused` variant.

Both block the codemod for the ProgressBar component but are smaller than DC-005 / DC-008.
