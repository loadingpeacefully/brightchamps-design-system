# DC-009 + DC-010 Filed — Verification Screenshots

**Date:** 2026-04-26
**Trigger:** Component-spec verification against the dashboard repo (see `docs/component-spec-verification-dashboard.md`) surfaced two new color values shipping in production that aren't in the ledger. Both filed as new drift tickets.

| # | File | Shows |
|---|---|---|
| 01 | [01-dc009-progress-success.png](2026-04-26-dc009-dc010/01-dc009-progress-success.png) | DC-009 — "Progress success color not in ledger". MEDIUM severity, ΔE 8. Two green swatches side by side: Designer #00B67A vs Production #11ac69. Action: update `ProgressBar` completed fill to canonical success/500 (also fixes `$success-green` and `$completed-state` in dashboard `_variables.scss`). Notes link to DC-001 — both should resolve together. |
| 02 | [02-dc010-paused-state.png](2026-04-26-dc009-dc010/02-dc010-paused-state.png) | DC-010 — "Paused state color not in ledger". LOW severity. Designer slot shows "no match" italic (no canonical equivalent yet); Production swatch is blue `#3b9af5`. Action: design team to decide — add as `color/info/600` or map to nearest existing info token. |

Section header automatically reads "10 tickets" (was 8 before this change). DC-005 (critical) and DC-008 (typo) preserved unchanged.
