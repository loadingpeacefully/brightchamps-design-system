# DC-005 Three-Way Conflict — Verification Screenshots

**Date:** 2026-04-26
**Trigger:** Source-code audit of `brightchamps-student-feed` revealed a third value (`#6651e4`) for the brand primary, in addition to the designer DS (`#722ED1`) and Figma extraction (`#4e3bc2`). DC-005 upgraded from HIGH to **CRITICAL**.

| # | File | Shows |
|---|---|---|
| 01 | [01-dc005-critical-three-way.png](2026-04-26-dc005-three-way/01-dc005-critical-three-way.png) | Element shot of DC-005. Shows new CRITICAL badge (deep red), 3-WAY CONFLICT purple badge, three swatches side by side (Designer DS `#722ED1` / Production · Figma `#4e3bc2` / Production · Code `#6651e4`), red note pointing at `docs/component-spec-verification.md`, updated action ("Block all other token work until resolved"), full notes preserving the source-code evidence. |
| 02 | [02-dc-section-critical-highlighted.png](2026-04-26-dc005-three-way/02-dc-section-critical-highlighted.png) | Full Designer-conflicts section in context. DC-005 stands out with a thick red border treatment because severity is now critical; all other tickets unchanged. |

## What changed

### `ledger/drift/designer-conflicts-2026-04-26.json` — DC-005 entry
- `title`: "Brand primary color" → "Brand primary color — three-way conflict"
- `severity`: `high` → `critical`
- Added `productionFigmaValue: "#4e3bc2"` (mirrors existing `productionValue`)
- Added `productionCodeValue: "#6651e4"`
- Added `threeWayConflict: true`
- `action`: tightened to "Block all other token work until resolved. This is the foundation everything builds on."
- `notes`: replaced with full three-way explanation citing the source-code evidence from `docs/component-spec-verification.md` and `docs/hardcoded-color-migration.md`

### `src/publish/ds-site-v2/lib/designer-conflicts.ts`
- `severity` union extended: `'critical' | 'high' | 'medium' | 'low'`
- Added optional `productionFigmaValue?`, `productionCodeValue?`, `threeWayConflict?` fields

### `src/publish/ds-site-v2/app/(docs)/surfaces/page.tsx`
- `SeverityBadge`: added `critical` case (deep red bg `rgba(99,9,21,0.92)`, white text, red border) — visually distinct from the existing `high` (light red bg, dark red text)
- New `Swatch` helper extracted for reuse
- `ConflictRow`: when `threeWayConflict === true && category === 'color'`, renders three swatches in a 3-column grid (Designer DS / Production · Figma / Production · Code) instead of two; adds a red `docs/component-spec-verification.md` reference line below the swatches; outer card border switches to thick red when severity is critical
- Existing 2-swatch path preserved for the other 6 tickets — no regression
